--!strict

-- [[ Refresh Server ]] ----------------------------------------
local RefreshServer = {}

type BaseService = {}

function RefreshServer.RegisterService(service: BaseService)
	print(service)
	error("TODO: not yet implented")
end

return RefreshServer
